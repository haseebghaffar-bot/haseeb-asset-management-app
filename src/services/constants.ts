import { AssetCategory, AssetStatus, AssetCondition } from '@/types';

export const CATEGORY_OPTIONS = Object.values(AssetCategory);
export const STATUS_OPTIONS = Object.values(AssetStatus);
export const CONDITION_OPTIONS = Object.values(AssetCondition);
